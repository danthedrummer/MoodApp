package org.dandowney.bestpartofmyday.feature.settings

/**
 * Settings screen so the user can modify behaviour of the app. Not exactly sure what I would
 * put here just yet. Dark mode seems a bit useless since the theming is mood driven and the
 * date is written in a locale agnostic way currently.
 */